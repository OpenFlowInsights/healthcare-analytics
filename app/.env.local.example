# Snowflake Connection Configuration
# Copy this file to .env.local and update values if needed

# Snowflake Account (format: account.region)
SNOWFLAKE_ACCOUNT=jic51019.us-east-1

# Snowflake User (must have key-pair auth configured)
SNOWFLAKE_USERNAME=APP_SERVICE

# Database and Schema
SNOWFLAKE_DATABASE=DEV_DB
SNOWFLAKE_SCHEMA=MSSP_ACO

# Warehouse and Role
SNOWFLAKE_WAREHOUSE=DEV_WH
SNOWFLAKE_ROLE=ACCOUNTADMIN

# Note: Private key is read from rsa_key.p8 file
# No password needed - using JWT key-pair authentication

# Site Access Password Protection (temporary, remove when ready to launch)
SITE_ACCESS_PASSWORD=your-secure-password
SITE_ACCESS_TOKEN=generate-a-random-token-here
